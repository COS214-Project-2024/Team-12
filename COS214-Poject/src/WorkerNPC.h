// // WorkerNPC.h
// #ifndef WORKERNPC_H
// #define WORKERNPC_H

// #include "CollectionStrategy.h"
// #include <string>
// #include <memory>

// class WorkerNPC {
// private:
//     std::string name;
//     double cost;
//     std::unique_ptr<CollectionStrategy> strategy;

// public:
//     WorkerNPC(std::string name, double cost, std::unique_ptr<CollectionStrategy> strategy);
//     std::string getName() const;
//     double getCost() const;
//     int collect(int baseAmount) const;
// };

// #endif
